# 2026-08-26

## 作品展示区块改造（两轮）

项目：movie-design-web（Astro 单页深色摄影工作室 Demo）。作品展示由 `Works.astro` + `global.css` 的 `.work-*` + `index.astro` 内联脚本驱动。

### 第一轮（已废弃）
- 用户原先对 4 列等宽横滚 + 栏目筛选只耦合在 Hero 的方案不满意。
- 我做过一版「编辑式 Bento 网格 + 区块内筛选 Pill」改造稿（画布 `作品展示改造方案`），并开了分支 `feature/works-bento-redesign`。
- 用户已退回 main，未采用 Bento 方向。

### 第二轮（当前）
- 用户要「仍是轮播图，但有层级」→ 明确为 **中心聚焦 Cover Flow**：焦点作品居中放大、两侧卡片缩小变暗 + 轻微 3D 旋转层叠（z 轴景深）。
- 新分支 `feature/works-coverflow`（基于 main）。
- 改动文件：
  - `src/components/Works.astro`：新增区块内栏目 Pill（`data-channel-tab`）、`.work-stage`/`.work-track` 结构与每张卡 `data-work-channel`、`.work-gallery` 上 `data-default-channel`。
  - `src/styles/global.css`：替换 `.work-gallery/.work-track/.work-card` 为封面流（absolute 居中 + perspective + 由 JS 设置 transform/opacity/zIndex），新增 `.work-filters/.work-pill/.work-tag/.work-cta`；响应式改 `.work-stage` 高度，去掉失效的 `grid-auto-columns`。
  - `src/pages/index.astro`：给 `Works` 传 `channels={page.hero.channels}`；把旧 `activateChannel` 横滚逻辑替换为统一的 `setChannel`（Hero Tab 与作品 Pill 共用，同时驱动 Hero 特性面板 + 封面筛选），并加封面流控制器（layout/go/点击侧卡聚焦/键盘左右/进入视口重排）。
- 关键设计：首屏默认显示 `video` 栏目 6 件，焦点卡居中放大、其余按距离缩小压暗后退；点侧卡即切换焦点，圆形上一件/下一件按钮驱动 `go()`。
- 修复原 bug：旧方案作品区无独立筛选器、与 Hero Tab 不联动；现统一 `setChannel` 同步两边状态。
- 验证：Node 22 构建通过（生成 index.html + 24 个作品详情页）；`npm run build` 末尾的 `safe-delete` 报错是 WorkBuddy 沙箱回收 `dist/pages/*.mjs` 失败，不影响产物。本地 dev `http://localhost:4321/` 已起。

### 第三轮（交互增强，第二轮延续）
- 用户反馈：cover flow 仅靠左右圆形按钮切换"有点傻、不符合使用习惯"。
- 新增最直觉的手势交互（`src/pages/index.astro` 控制器 + `src/styles/global.css`）：
  - **指针拖拽 / 触摸滑动**：pointerdown/move/up 跟手平移 `.work-track`（`translateX`），松手按位移阈值（首卡宽 *0.22）吸附到上/下一件，未超阈值则回弹；用 `setPointerCapture` 捕获指针。
  - **拖拽误触拦截**：拖拽后通过 capture 阶段 click 监听器拦截误触发的卡片点击（避免拖动焦点卡却进详情）。
  - **滚轮横向**：仅响应 trackpad 横向滑动（`|deltaX|>|deltaY|`）切卡并 `preventDefault`，纵向滚轮放行页面；加 320ms 节流锁。
  - 圆形按钮降为辅助，手势为主。
- CSS：`.work-track` 加 `touch-action:pan-y`（横向手势归我们、纵向放行页面）+ `cursor:grab`；`.work-track.is-grabbing` 时 `cursor:grabbing`。
- 验证：Node 22 构建无真实编译错误（bundle 含 `pointerdown`/`is-grabbing`）；本地 dev 仍在 `http://localhost:4321/`（HTTP 200，HMR 已生效）。

### 第三轮 · 层级 + 模糊 + 贴边箭头 + 步进一格
参考用户截图（Clipboard_Screenshot.png）将 Cover Flow 改造成"焦点最清晰、侧卡模糊压暗"的景深风格。
- **Works.astro**：移除 SectionHeading 的左右圆按钮（不传 controlsId），改为在 `.work-stage` 内挂两个浮动箭头按钮（`.work-arrow-prev/next`，绝对定位在 stage 左右两端各 24px）。
- **global.css**：新增 `.work-arrow` 圆形按钮样式（hover 切到金色边框 + 半透明金色底，带 backdrop-filter 模糊底色）。
- **index.astro / layout()**：`card.style.filter = dist === 0 ? '' : \`blur(\${dist * 4}px)\``，侧卡糊得只剩焦点清晰；并把点击侧卡的逻辑从"跳到该卡"改为 `go(idx > activeIndex ? 1 : -1)` —— 朝被点击方向步进一格。
- 新增 `[data-arrow]` 监听，让 stage 内新箭头驱动 `go()`。
- 构建退出码 0，无真实编译错误；产物含 `work-arrow`×4 / `data-arrow`×2；dev 200。

### 第四轮 · 焦点封面放大 + info 淡入 + 焦点卡可拖
- 用户指出焦点卡拖不动（原生 `<a>` 链接拖拽抢手势）。`Works.astro` 给 `.work-card` 与 `<img>` 加 `draggable="false"` 堵掉原生 DnD；`global.css` 给 `.work-card` 加 `user-select:none` 防拖拽时选中文字。
- 聚焦瞬间强化：`.work-card.is-active > img` 封面放大 `scale(1.05)→1.08`；`.work-copy` 在非焦点态 `opacity:.32 + translateY(10px)`，焦点态淡入归位（标题+CTA 整体随焦点浮现，CTA 额外延迟 .08s）。
- 焦点卡拖拽由 worksTrack 的 pointer 事件统一接管（drag 阈值过则 go(±1)，未过则 suppressClick 拦截误触导航），现已可"抓起焦点卡就甩"。
- 构建 EXIT=1 仅为 genie-safe-delete 回收 dist 临时文件失败的沙箱噪音；产物 dist/index.html 时间戳 21:49 新鲜，CSS bundle 含 scale(1.08)/user-select:none/work-copy，HTML 含 draggable；dev 200。
- 初始焦点微调：默认 `activeIndex` 改为第 3 张（`INITIAL_FOCUS=2`，clamp 到栏目长度-1），使左侧露出两张、构图平衡；切换栏目时亦沿用该默认焦点。

## 首页分类/作品信息架构重设计稿

- 时间：21:33 左右。
- 在 Ardot 创建设计稿 `首页重设计-方向一`（fileId 719077733068784）。
- 针对之前诊断的“分类（channels）在 Hero 与作品区重复出现并共用同一套 setChannel 筛选”的问题，按方向一出稿：Hero 退为纯品牌瞬间、新增“业务线概述”区块承载分类要点、作品展示区成为全站唯一筛选点。
- 业务线重构为平行分类：商业影像 / 人物纪实 / 空间建筑 / 影像科普。
- 视觉语言沿用项目既定：深色基底 + 居中标题 + 金色点缀 + 大幅摄影。
- 画布结构：Header、Hero（纯品牌标题+播放键）、业务线概述（4 卡片×3 要点）、作品展示（分段控件 + 3×2 网格）、Footer。
- AI 生成作品图 6 张用于网格；已截图验证（Hero/业务线/作品/Footer 四个区块正常）。

### 高级摄影师换东方面孔
- 用户要求把"高级摄影师"两张人像（home.ts `photographers` 数组前两条：张风、王识宇）改成东方面孔。
- 原图是 Unsplash 远程图，替换成本地资源：`/photographers/zhangfeng.jpg`、`/photographers/wangshiyu.jpg`。
- 流程：ImageGen 文生图（1024×1536, high quality，东方面孔 + 站点深色+金色编辑调性）→ Pillow 裁底部 120px 去掉"AI生成 WORKBUDDY"平台水印 → 转 JPEG 改 .jpg 扩展名 → 更新 home.ts portrait/alt → dev HMR 自动接入。
- 工具链：托管 Python 3.13.12 建 venv `~/.workbuddy/binaries/python/envs/default` 装 Pillow 12.3.0（环境无 IM/Pillow 时按系统规则走 venv）。ImageGen 每张 5–10 额度，两张共约 10–20。
- 验证：dev 页面摄影师区 `src` 已切到本地 `/photographers/*.jpg`，老 Unsplash photographer ID 不再出现在该区（残留 1 处在某 works 封面，不属本次范围）；Node 22 构建 EXIT=1 仍为末尾 safe-delete 沙箱噪音（真实错误 grep 为空）。
- 备注：`.person-photo` 容器 `aspect-ratio:1.2/1` + `object-fit:cover` 会上下裁切约 22%，底部水印大概率已被裁掉，但为源文件干净仍裁。
