# 后台服务端口
PORT=3001

# Session Cookie 签名密钥（部署时必须改成随机字符串）
SESSION_SECRET=change-me-to-a-random-secret

# 首次启动时的种子管理员账号（仅在 users 表为空时生效）
ADMIN_INITIAL_PASSWORD=admin123

# 站点对外域名（用于拼接限时链接，留空则按请求 Host 拼接）
SITE_URL=
